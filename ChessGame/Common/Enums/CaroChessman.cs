namespace Common.Enums
{
    public enum CaroChessman
    {
        /// <summary>
        /// Default value, need to be 0
        /// </summary>
        Empty = 0,
        X = 1,
        O = -1
    }
}
